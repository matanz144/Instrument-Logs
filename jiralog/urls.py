from django.urls import path

from . import views

app_name = 'compound'
urlpatterns = [
    # path('list/', views.LogList.as_view(), name='list-all-logs'),
    # path('new/', views.LogCreate.as_view(), name='new-log'),
    # path('test/', views.TestView.as_view(), name='test'),
]
